import { CampaignOrbitalBody } from './campaign-orbital-body';

describe('CampaignOrbitalBody', () => {
  it('should create an instance', () => {
    expect(new CampaignOrbitalBody()).toBeTruthy();
  });
});
